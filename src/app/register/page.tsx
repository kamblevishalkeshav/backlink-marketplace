'use client';

// Import the actual register component from the (auth) directory
import RegisterPage from '../(auth)/register/page';

// Re-export it as the default export for this route
export default RegisterPage; 