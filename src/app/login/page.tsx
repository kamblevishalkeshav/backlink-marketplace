'use client';

// Import the actual login component from the (auth) directory
import LoginPage from '../(auth)/login/page';

// Re-export it as the default export for this route
export default LoginPage; 